{
    "name": "sammy",
    "version": "1.0.0",
    "type": "module",
    "main": "index.js",
    "author": "m00chle",
    "license": "MIT",
    "scripts": {
        "start": "node .",
        "install-client": "cd client && yarn",
        "install-all": "yarn && yarn install-client",
        "server": "nodemon .",
        "client": "cd client && yarn start",
        "heroku-postbuild": "heroku config:set YARN_PRODUCTION=true && cd client && yarn && yarn build"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "discord.js": "^12.2.0",
        "dotenv": "^8.2.0",
        "express": "^4.17.1"
    },
    "devDependencies": {
        "nodemon": "^2.0.4"
    }
}
